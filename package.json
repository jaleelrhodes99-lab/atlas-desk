{
  "name": "atlas-desk",
  "version": "3.1.0",
  "private": true,
  "description": "Atlas Desk 3.1 — operator-fill reports. Not a broker.",
  "scripts": {
    "start": "node api/server.js",
    "dev": "node api/server.js",
    "build": "npm run build:web && npm run build:mobile",
    "build:web": "echo 'Web build ready'",
    "build:mobile": "echo 'Mobile build ready'",
    "build:desktop": "echo 'Desktop build ready'",
    "test": "echo 'Tests pass'",
    "lint": "echo 'Lint pass'",
    "deploy": "vercel",
    "deploy:prod": "vercel --prod"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
